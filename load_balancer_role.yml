---
- name: Create and configure Aazure internal load balancer for SQL AOAG
  hosts: localhost
  gather_facts: false
  vars_files:
    - roles/load-balancer2/vars/lb_config_params1.yml
  roles:
    - pre-checks1
